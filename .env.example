# Secure Messenger - Environment Variables Template
# Copy this file to .env and replace placeholder values with actual secrets
# Usage: cp .env.example .env

# ============================================
# DATABASE CONFIGURATION
# ============================================
POSTGRES_USER=messenger_admin
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_MIN_32_CHARS
POSTGRES_DB=messenger_db

# ============================================
# REDIS CONFIGURATION
# ============================================
# Leave empty for no password (development only)
REDIS_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_MIN_32_CHARS

# ============================================
# RABBITMQ CONFIGURATION
# ============================================
RABBITMQ_USER=messenger
RABBITMQ_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_MIN_32_CHARS

# ============================================
# JWT AUTHENTICATION
# ============================================
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_64_CHARACTERS_FOR_PRODUCTION
JWT_ISSUER=MessengerAuthService
JWT_AUDIENCE=MessengerClient
JWT_EXPIRY_MINUTES=60

# ============================================
# APPLICATION ENVIRONMENT
# ============================================
ASPNETCORE_ENVIRONMENT=Development

# ============================================
# SMTP CONFIGURATION (Optional - for email notifications)
# ============================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-app-specific-password

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER commit the actual .env file to version control
# 2. Use strong, random passwords (min. 32 characters)
# 3. Generate JWT secret with: openssl rand -base64 64
# 4. For production, use environment variables or secret managers
# 5. Rotate secrets regularly (every 90 days recommended)

# ============================================
# GENERATING SECURE SECRETS
# ============================================
# PowerShell (Windows):
#   -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 64 | ForEach-Object {[char]$_})
#
# Bash (Linux/macOS):
#   openssl rand -base64 64 | tr -d '\n'
#
# Online (use with caution):
#   https://www.random.org/passwords/

# ============================================
# EXAMPLE VALUES (FOR DEVELOPMENT ONLY)
# ============================================
# POSTGRES_PASSWORD=dev_password_not_for_production
# REDIS_PASSWORD=dev_redis_password
# RABBITMQ_PASSWORD=dev_rabbitmq_password
# JWT_SECRET=dev_jwt_secret_min_64_chars_aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
