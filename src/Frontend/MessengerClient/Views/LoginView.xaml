<!-- ========================================
     PSEUDO-CODE - Sprint 7: Login View
     Status: 🔷 XAML-Struktur vervollständigt mit MaterialDesign
     ======================================== -->
<UserControl x:Class="MessengerClient.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:MessengerClient.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="400"
             d:DataContext="{d:DesignInstance Type=viewModels:LoginViewModel}">
    
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="350">
            
            <!-- Logo -->
            <materialDesign:PackIcon Kind="MessageLock" 
                                     Width="100" Height="100" 
                                     HorizontalAlignment="Center" 
                                     Foreground="{DynamicResource PrimaryHueMidBrush}"
                                     Margin="0,0,0,20"/>
            
            <!-- Title -->
            <TextBlock Text="Secure Messenger" 
                      FontSize="32" 
                      FontWeight="Bold" 
                      HorizontalAlignment="Center" 
                      Margin="0,0,0,40"
                      Foreground="{DynamicResource MaterialDesignBody}"/>
            
            <!-- Email Input -->
            <TextBox x:Name="EmailTextBox"
                     materialDesign:HintAssist.Hint="E-Mail oder Benutzername" 
                     Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,0,20"/>
            
            <!-- Password Input -->
            <PasswordBox x:Name="PasswordBox"
                        materialDesign:HintAssist.Hint="Passwort"
                        Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                        Margin="0,0,0,20"/>
            
            <!-- MFA Input (Visible only if MfaRequired) -->
            <StackPanel Visibility="{Binding MfaRequired, Converter={StaticResource BoolToVisibilityConverter}}"
                       Margin="0,0,0,20">
                
                <TextBlock Text="Zwei-Faktor-Authentifizierung"
                          FontWeight="Bold"
                          Margin="0,0,0,10"
                          Foreground="{DynamicResource MaterialDesignBody}"/>
                
                <TextBox materialDesign:HintAssist.Hint="6-stelliger Code" 
                         Text="{Binding MfaCode, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         MaxLength="6"
                         HorizontalContentAlignment="Center"
                         FontSize="18"
                         FontFamily="Consolas"/>
                
                <!-- Available MFA Methods Info -->
                <TextBlock Text="Code aus Ihrer Authenticator-App eingeben"
                          Foreground="{DynamicResource MaterialDesignBodyLight}"
                          FontSize="12"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
            </StackPanel>
            
            <!-- Error Message -->
            <Border Background="#FFEBEE" 
                    BorderBrush="#F44336" 
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="12"
                    Margin="0,0,0,15"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Alert" 
                                             Width="20" Height="20" 
                                             Foreground="#F44336"
                                             Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding ErrorMessage}" 
                              Foreground="#F44336"
                              TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
            
            <!-- Loading Indicator -->
            <ProgressBar IsIndeterminate="True"
                        Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                        Margin="0,0,0,20"/>
            
            <!-- Login Button -->
            <Button Content="ANMELDEN" 
                   Command="{Binding LoginCommand}"
                   Style="{StaticResource MaterialDesignRaisedButton}"
                   Background="{DynamicResource PrimaryHueMidBrush}"
                   Height="45"
                   FontWeight="Bold"
                   Margin="0,20,0,10"
                   Visibility="{Binding MfaRequired, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
            
            <!-- Verify MFA Button -->
            <Button Content="CODE VERIFIZIEREN" 
                   Command="{Binding VerifyMfaCommand}"
                   Style="{StaticResource MaterialDesignRaisedButton}"
                   Background="{DynamicResource SecondaryHueMidBrush}"
                   Height="45"
                   FontWeight="Bold"
                   Margin="0,20,0,10"
                   Visibility="{Binding MfaRequired, Converter={StaticResource BoolToVisibilityConverter}}"/>
            
            <!-- Register Link -->
            <StackPanel Orientation="Horizontal" 
                       HorizontalAlignment="Center"
                       Margin="0,20,0,0">
                <TextBlock Text="Noch kein Konto? " 
                           Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                <Button Content="Registrieren" 
                       Command="{Binding NavigateToRegisterCommand}"
                       Style="{StaticResource MaterialDesignFlatButton}"
                       Foreground="{DynamicResource PrimaryHueMidBrush}"
                       FontWeight="Bold"/>
            </StackPanel>
            
            <!-- Additional Options -->
            <StackPanel Margin="0,20,0,0">
                <Button Content="Passwort vergessen?" 
                       Style="{StaticResource MaterialDesignFlatButton}"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       FontSize="12"/>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</UserControl>
