<!-- PSEUDO-CODE - Sprint 8: Chat View -->
<UserControl x:Class="SecureMessenger.Client.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    
    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Background="{DynamicResource MaterialDesignDarkBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Konversationen" 
                      FontSize="20" 
                      FontWeight="Bold" 
                      Margin="20,20,20,10"/>

            <ListBox Grid.Row="1" 
                    ItemsSource="{Binding Conversations}"
                    SelectedItem="{Binding SelectedConversation, Mode=TwoWay}"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" 
                                      Text="{Binding Name}" 
                                      FontWeight="Bold" 
                                      FontSize="14"/>
                            
                            <Grid Grid.Row="1" Margin="0,5,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" 
                                          Text="{Binding LastMessage}" 
                                          FontSize="12" 
                                          Foreground="Gray"
                                          TextTrimming="CharacterEllipsis"/>
                                
                                <TextBlock Grid.Column="1" 
                                          Text="{Binding LastMessageTime, Converter={StaticResource TimestampConverter}}" 
                                          FontSize="10" 
                                          Foreground="Gray"
                                          Margin="5,0,0,0"/>
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="16">
                <TextBlock Text="{Binding SelectedConversation.Name}" 
                          FontSize="18" 
                          FontWeight="Bold"/>
            </materialDesign:ColorZone>

            <ScrollViewer Grid.Row="1" 
                         x:Name="MessageScrollViewer"
                         VerticalScrollBarVisibility="Auto"
                         Padding="10">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding IsSent, Converter={StaticResource MessageBackgroundConverter}}"
                                   CornerRadius="10" 
                                   Padding="12" 
                                   Margin="8,4"
                                   MaxWidth="400"
                                   HorizontalAlignment="{Binding IsSent, Converter={StaticResource MessageAlignmentConverter}}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Content}" 
                                              TextWrapping="Wrap" 
                                              Foreground="White"/>
                                    <TextBlock Text="{Binding Timestamp, Converter={StaticResource TimestampConverter}}" 
                                              FontSize="10" 
                                              Foreground="LightGray" 
                                              Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <Grid Grid.Row="2" Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                       Style="{StaticResource MaterialDesignIconButton}"
                       Command="{Binding SendFileCommand}"
                       ToolTip="Datei anhängen">
                    <materialDesign:PackIcon Kind="Attachment"/>
                </Button>

                <TextBox Grid.Column="1" 
                        Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="Nachricht eingeben..."
                        Style="{StaticResource MaterialDesignOutlinedTextBox}"
                        Margin="10,0"
                        AcceptsReturn="False"/>

                <Button Grid.Column="2" 
                       Content="SENDEN"
                       Command="{Binding SendMessageCommand}"
                       Style="{StaticResource MaterialDesignRaisedButton}"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
